node_modules
dist
coverage
.DS_Store
scratchpad

# Local design reference exports, not part of the package.
# Anchored to the repo root: an unanchored `input/` also matched
# src/components/input/ and silently ignored new component files.
/input/
/❖ Design System Altics 2.0(3)/

# Shallow clones of other design systems, read for reference only.
/refs/
